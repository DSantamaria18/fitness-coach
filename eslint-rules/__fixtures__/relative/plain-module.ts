// Fixture: módulo sin directiva ("server-safe" por defecto en RSC), usado
// por no-client-import-in-server-file.test.ts para el caso válido de
// import relativo a un fichero normal.
export const helper = (): string => "server-safe value";
